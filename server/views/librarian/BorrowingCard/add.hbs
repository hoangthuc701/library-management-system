{{#section 'js'}}
<script>
  
</script>
{{/section}}

<div class="card">
    <div class="card-header">
        <h4>Thêm phiếu mượn</h4>
    </div>
    <div class="row" style="margin-top: 20px;">
        <div class="col-sm-1"></div>
        <div class="col-sm-9">
        <form method="POST" action="" style="width: 100%" id="form-confirm">
            <div class="form-group">
                <label for="Crole">Độc giả</label>
                <select id="cbReader" name="readerID" class="form-control" onchange="cbRoleChange({{id}})">
                    {{#each this.List}}
                    <option value="{{id}}" {{#when id 'eq' 1}}selected{{/when}}>{{username}}</option>
                    {{/each}}
                </select>
            </div>
            <div class="form-group">
                <label for="Cusername">Thời hạn</label>
                <input type="date" class="form-control" id="returned_date" name="returned_date" placeholder=""
                    value="">
            </div>
            <a class="btn btn-success" href="/librarian/BorrowingCard?p=1" role="button" title="Back to List">
                <i class="fa fa-backward" aria-hidden="true"></i>
            </a>
            <input type="submit" value="Submit" class="btn btn-primary" id="btn-confirm" >
        </form>
        </div>
        <div class="col-sm-2"></div>
    </div>
</div>